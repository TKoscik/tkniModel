# tkniModel

`tkniModel` is an R package designed for parallelized voxel-wise analysis of NIfTI data, specifically optimized for Cluster/HPC environments.

## Installation

```r
# Install from source
install.packages("devtools")
devtools::install_github("TKoscik/tkniModel")
